export const environment = {
    production: false,
    quality: false,
    local: false,
    api: {
        version: 'v1',
        baseURL: '/example/', // replace with your API endpoint
        // Add your API endpoints here
    }
};